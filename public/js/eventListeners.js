// some examples of event listeners for later use


// addEventListener('click', (event) => {
//   randCol = Math.floor(Math.random() * 360)
//   myPlayers[socket.id].color = 'hsl(' + randCol + ', 100%, 50%)'
//   io.emit('currentPlayers', myPlayers)
// })

// window.addEventListener("keydown", (event) => {
//   switch (event.code) {
//     case "KeyW":
//       socket.emit('keydown', { direction: 'KeyW' })
//       break
//     case "KeyA":
//       socket.emit('keydown', { direction: 'KeyA' })
//       break
//     case "KeyS":
//       socket.emit('keydown', { direction: 'KeyS' })
//       break
//     case "KeyD":
//       socket.emit('keydown', { direction: 'KeyD' })
//       break
//   }
// })

// window.addEventListener("keyup", onKeyUp, false);

